# Copy this file to .streamlit/secrets.toml and fill in your key.
# secrets.toml is gitignored and never committed.

# RECOMMENDED (free, works on Streamlit Cloud): get from console.groq.com
GROQ_API_KEY = "gsk_..."

# Alternatives (only one key is needed):
# GEMINI_API_KEY = "AIzaSy..."   # Google AI Studio — free but may have network issues on cloud
# OPENAI_API_KEY = "sk-..."      # OpenAI — paid
